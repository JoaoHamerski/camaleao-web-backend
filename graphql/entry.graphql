type Entry @guard {
  id: ID!
  bank_uid: String!
  value: Float!
  date: String!
  description: String
  is_canceled: Boolean
  via_id: ID
}

enum QueryWhereEntriesColumns {
  VALUE @enum(value: "value")
  DESCRIPTION @enum(value: "description")
}

extend type Query {
  entries(
    where: _ @whereConditions(columnsEnum: "QueryWhereEntriesColumns")
  ): [Entry] @paginate

  entriesMonthlyBalance: Float
}

extend type Mutation {
  entryCancel(uid: ID!): Entry!
}
